{
  "name": "latex-equation2image-easy2copy",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "cf:install": "curl -LsSf https://astral.sh/uv/install.sh | sh && $HOME/.local/bin/uv sync",
    "cf:build": "$HOME/.local/bin/uv run pywrangler versions upload",
    "cf:deploy": "$HOME/.local/bin/uv run pywrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.57.0"
  }
}
