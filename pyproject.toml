[project]
name = "latex-equation2image-easy2copy"
version = "0.1.0"
description = "Deployable on Cloudflare Python Workers"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.116.0",
  "httpx>=0.28.0",
  "python-multipart>=0.0.20"
]

[dependency-groups]
dev = [
  "workers-py>=0.0.21"
]
